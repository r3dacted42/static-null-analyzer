# client

- provides an editor using CodeMirror and renders the CFG (dot) using viz-js
- sends entered code and function name to server and displays the result
- supports light/dark themes for the ui and the CFG

## how to run

```bash
# install deps
npm i

# run dev server
npm run dev
```
